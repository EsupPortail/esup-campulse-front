server {
    listen 80;
    server_name oniros.unistra.fr oniros;

    rewrite             ^ https://$server_name$request_uri? permanent;
}


server {
    listen 443 ssl;
    ssl_certificate      /etc/ssl/certs/mega_wildcard.pem;
    ssl_certificate_key  /etc/ssl/private/mega_wildcard.key;
    server_name  oniros.unistra.fr  oniros;
    charset utf-8;
    root    /var/www/static/oniros-app;
    # redirect 404 page to index.html
    error_page  404 /index.html;
    # Always serve index.html for any request
    location / {
      try_files $uri $uri/ @rewrites;
    }

    location @rewrites {
      rewrite ^(.+)$ /index.html last;
    }
    error_log  /var/log/nginx/oniros.unistra.fr-error.log;
    access_log /var/log/nginx/oniros.unistra.fr-access.log;
}
